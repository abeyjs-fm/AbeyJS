# Copiá a `.env.development` / `.env.qa` / `.env.production`
# En Vite, solo se exponen variables que empiezan con `VITE_`.

VITE_APP_ENV=dev
VITE_API_URL=http://localhost:3000
VITE_OPENAPI_URL=http://localhost:3000/openapi.json
# Inspector (opcional, solo con hub en marcha):
# VITE_INSPECTOR_HUB=ws://127.0.0.1:7071

